import 'package:flutter/material.dart';

class BossDashboard extends StatelessWidget {
  const BossDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Boss Dashboard", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
